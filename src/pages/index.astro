<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/flores-amarillas/yellow-flower-icon-sign-symbol-png.webp" />
		<meta name="generator" content={Astro.generator} />
		<title>Flores Amarillas</title>
	</head>
	<body class="love-font bg-yellow-100 flex flex-col items-center justify-center h-svh w-svw">
		<div class="bg-img flex flex-col items-center justify-center my-5 mx-5 rounded-3xl p-5">
			<div class="card bg-yellow-200 shadow-xl rounded-3xl m-5">
				<h1 class="text-3xl mt-5 text-center">🌸 Flores Amarillas 🌸</h1>
				<img src="/flores-amarillas/Sunflower-removebg-preview.png" class="max-h-96">
				<h1 class="love-font text-3xl mt-5 text-center mb-5">🌸 Para Ti 🌸</h1>
			</div>
		</div>
		<div id="text-introduction" class="bg-black text-5xl text-white absolute top-0 left-0 h-svh w-svw p-5 z-50 grid place-content-center">
			<h2>¿Un click? c:</h2>
		</div>
	</body>
</html>

<script>
	const textIntroduction = document.getElementById('text-introduction');
	textIntroduction?.addEventListener('click', () => {
		textIntroduction.innerHTML = 'Para ti';
		textIntroduction.style.transition = 'all ease-in-out 3s';
		textIntroduction.classList.add('fadeOut');
		textIntroduction.style.opacity = '0';
		textIntroduction.style.pointerEvents = 'none';
	});

	// Card rotate to left if mouse enter from right and vice versa
	const card = document.querySelector('.card') as HTMLElement;
	card?.addEventListener('mouseenter', (event) => {
		const { clientX } = event as MouseEvent;
		const { left, right } = card.getBoundingClientRect();
		const isEnterFromRight = clientX > (left + right) / 2;
		card.style.transform = `rotate(${isEnterFromRight ? 1 : -1}deg)`;
		card.style.transition = 'all ease-in-out 0.3s';
		card.style.filter = 'drop-shadow(0 0 0.75rem rgb(255, 238, 0))';
		card.style.scale = '1.01';
	});

</script>

<style>
	@font-face {
		font-family: 'love-font';
		src: url('/flores-amarillas/Love.woff2') format('woff2'),
	}

	.love-font {
		font-weight: bolder;
		font-family: 'love-font', sans-serif;
	}

	.bg-img {
		background-image: url("/flores-amarillas/bg6.png");
		background-size: contain;
		transition: all ease-in-out 0.3s;
	}
</style>
